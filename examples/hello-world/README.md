# Hello World Scripted Connector

## Project Structure

The source code for the "hello world" scripted connector can be found in the [`src`](./src/) folder. That folder contains everything necessary to package the scripted connector. The packaged end-result can be found in the [`dist`](./dist/) folder.