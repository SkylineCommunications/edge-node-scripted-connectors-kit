# Examples

A collection of scripted connector examples to help you get started.

## Structure

Each example follows the same layout:

- **`src/`** — The full source code of the scripted connector
- **`dist/`** — The connector in its packaged `.dmscript` format, ready to deploy
